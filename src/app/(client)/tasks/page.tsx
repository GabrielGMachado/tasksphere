import { renderTasks, addTask, deleteTask } from "@/app/api/tasks/route";

export default function () {
    return (
        <div>
            <form>
              

            </form>
        </div>
    );
}